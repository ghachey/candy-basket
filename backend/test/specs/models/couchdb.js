'use strict';

var assert = require('assert');
var db = require('../../../app/models/couchdb');

// Write test to a fake couchdb backend?!

// describe('CouchDB Document Wrapper', function(){

//   describe('getFromCouch', function(){
//     it('get data from several docs and merge them as one', function(done){

//       db.getFromCouch('base.home.en-GB', function(err, body) {
//         if (err) {throw err;};
//         assert.deepEqual(body,{});
//         done();
//       });

//     });
//   });

// });
